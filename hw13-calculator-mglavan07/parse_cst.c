#include "cst.h"

#include <stdlib.h>
#include <string.h>

struct CST_node *parse_expr(struct stream *s) { return NULL; }

struct CST_node *parse_expr_rest(struct stream *s) { return NULL; }

struct CST_node *parse_term(struct stream *s) { return NULL; }
struct CST_node *parse_term_rest(struct stream *s) { return NULL; }

struct CST_node *parse_factor(struct stream *s) { return NULL; }

void free_cst(struct CST_node *cst) {}
